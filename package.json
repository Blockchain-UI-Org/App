{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "cd packages/bui-docs && yarn build",
    "start": "cd packages/bui-docs && yarn start"
  },
  "devDependencies": {
    "lerna": "^6.4.1"
  }
}

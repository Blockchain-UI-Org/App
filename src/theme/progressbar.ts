type IProgressBarVariant = {
  midtone: string;
};
export type IProgressBarTheme = {
  variants: {
    default: IProgressBarVariant;
    green: IProgressBarVariant;
    yellow: IProgressBarVariant;
    blue: IProgressBarVariant;
    red: IProgressBarVariant;
  };
};
